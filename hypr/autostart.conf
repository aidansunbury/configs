# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Import Wayland environment to systemd for services like hyprwhspr
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_RUNTIME_DIR
